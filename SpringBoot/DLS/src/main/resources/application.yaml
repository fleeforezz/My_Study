server:
  port: 8080
  # servlet:
  #   context-path: /api/v1

spring:
  application:
    name: DLS

  datasource:
    url: "jdbc:mysql://jsomysql.duckdns.org:32768/DLS"
    username: root
    password: TMNNT012928637160822863716

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true
